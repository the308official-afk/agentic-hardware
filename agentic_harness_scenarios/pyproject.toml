[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-harness-scenarios"
version = "0.1.0"
description = "Portable synthetic scenario lab for harness-aware inference signals."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
